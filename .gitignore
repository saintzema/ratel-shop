
# Environment & secrets
# Local Data & Enviroments
*-service-account.json
*.db
*.log
.DS_Store
.env
.env.local
.env.production
.next/
.vercel
backend/database/
backend/venv/
build/
database/
dist/
node_modules/
venv/
zemaai-service-account.json

# Large local-only assets — a `git add -A` swept these into a commit and the
# push failed with HTTP 400 (146 MB of decks and screenshots).
pitch-deck/
product-evidence/
.DS_Store
**/.DS_Store
# Stray file created by a mistyped `git branch > main` style redirect.
/main
