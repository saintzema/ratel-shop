import SellerBalancePage from "../wallet/page";

/**
 * Redirect/Alias for /seller/wallet to /seller/balance as requested by user.
 * This fixes the 404 error while maintaining the existing logic.
 */
export default SellerBalancePage;
